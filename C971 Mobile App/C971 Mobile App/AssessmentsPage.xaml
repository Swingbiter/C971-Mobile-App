<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="C971_Mobile_App.AssessmentsPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="COURSE NAME" x:Name="lbl_CourseName" HorizontalOptions="Center"/>
            <ListView x:Name="listView_Assessments" ItemTapped="listView_Assessments_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                                <Label Text="{Binding Type}" VerticalOptions="Center" TextColor="Black"/>
                                <Label Text="{Binding Title}" VerticalOptions="Center" TextColor="Black"/>
                                <Label Text="{Binding StartDate, StringFormat='{}{0:MM/dd/yyyy}'}" VerticalOptions="Center"/>
                                <Label Text="-" VerticalOptions="Center" HorizontalOptions="Center"/>
                                <Label Text="{Binding EndDate, StringFormat='{}{0:MM/dd/yyyy}'}" VerticalOptions="Center"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="Add Assessment" x:Name="btn_add_assess" Clicked="btn_add_assess_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>